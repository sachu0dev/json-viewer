# AI Search Readiness (GEO) — JSON Viewer

## Re-audit
- `/json-diff` and `/large-files` copy unchanged — still specific, quotable, good raw material for AI Overviews/Perplexity.
- **Homepage now has something for AI engines to work with:** `sr-only` H1 + page-specific `SoftwareApplication` schema (previously nothing but a meta description). Still no visible on-page paragraph copy, since the homepage's UI is a full-screen editor by design — schema + H1 is the ceiling here without a UX change to the tool itself.
- robots.txt still allows all AI crawlers (GPTBot, PerplexityBot, ClaudeBot) — unchanged, still a pass.

## Findings — re-audit

### High
None — resolved to the extent possible without redesigning the homepage into a landing page.

### Low
- No explicit brand-entity reinforcement (no About page) — unchanged, still low priority.

## Score: 75/100
